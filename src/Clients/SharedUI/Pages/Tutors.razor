@page "/tutors"
@using SharedUI.Components
@using SharedUI.Services
@using Clients.Infrastructure.Http
@inject ApiClient ApiClient
@inject IToastService Toast

<div class="header-section">
    <h1>Tutores</h1>
    <button class="btn btn-primary" @onclick="OpenAddModal">Novo Tutor</button>
</div>

<div class="card">
    <LoadingState IsLoading="_isLoading" Message="Carregando tutores...">
        <DataGrid Items="_tutors">
            <HeaderTemplate>
                <th>Nome</th>
                <th>Email</th>
                <th>Telefone</th>
                <th>Ações</th>
            </HeaderTemplate>
            <RowTemplate Context="tutor">
                <td>@tutor.Name</td>
                <td>@tutor.Email</td>
                <td>@tutor.Phone</td>
                <td>
                    <button class="btn btn-sm btn-info" @onclick="() => EditTutor(tutor)">Editar</button>
                </td>
            </RowTemplate>
        </DataGrid>
    </LoadingState>
</div>

<Modal @bind-IsVisible="_showModal" Title="@(_editingTutor == null ? "Novo Tutor" : "Editar Tutor")">
    <ChildContent>
        <FormField Label="Nome">
            <input type="text" @bind="_currentTutor.Name" class="form-control" />
        </FormField>
        <FormField Label="E-mail">
            <input type="email" @bind="_currentTutor.Email" class="form-control" />
        </FormField>
        <FormField Label="CPF">
            <input type="text" @bind="_currentTutor.Cpf" class="form-control" disabled="@(_editingTutor != null)" />
        </FormField>
        <FormField Label="Telefone">
            <input type="tel" @bind="_currentTutor.Phone" class="form-control" />
        </FormField>
    </ChildContent>
    <FooterTemplate>
        <button class="btn" @onclick="CloseModal">Cancelar</button>
        <button class="btn btn-primary" @onclick="SaveTutor">Salvar</button>
    </FooterTemplate>
</Modal>

@code {
    private List<TutorDto>? _tutors;
    private bool _showModal;
    private bool _isLoading;
    private TutorDto? _editingTutor;
    private TutorDto _currentTutor = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadTutorsAsync();
    }

    private async Task LoadTutorsAsync()
    {
        _isLoading = true;
        try
        {
            var result = await ApiClient.GetAsync<PagedResultDto<TutorDto>>("/api/v1/tutors?pageSize=100");
            if (result.IsSuccess)
            {
                _tutors = result.Value.Items.ToList();
            }
            else
            {
                _tutors = [];
                Toast.Show(result.Error.Message, ToastType.Error);
            }
        }
        finally
        {
            _isLoading = false;
        }
    }

    private void OpenAddModal()
    {
        _editingTutor = null;
        _currentTutor = new TutorDto();
        _showModal = true;
    }

    private void EditTutor(TutorDto tutor)
    {
        _editingTutor = tutor;
        _currentTutor = new TutorDto
        {
            Id = tutor.Id,
            Name = tutor.Name,
            Email = tutor.Email,
            Cpf = tutor.Cpf,
            Phone = tutor.Phone
        };
        _showModal = true;
    }

    private void CloseModal()
    {
        _showModal = false;
    }

    private async Task SaveTutor()
    {
        if (_editingTutor is null)
        {
            var id = Guid.NewGuid();
            var result = await ApiClient.PostAsync<CreateTutorRequest, Guid>(
                "/api/v1/tutors",
                new CreateTutorRequest
                {
                    Id = id,
                    Name = _currentTutor.Name,
                    Email = _currentTutor.Email,
                    Cpf = _currentTutor.Cpf,
                    Phone = _currentTutor.Phone
                },
                id);

            if (result.IsFailure)
            {
                Toast.Show(result.Error.Message, ToastType.Error);
                return;
            }
        }
        else
        {
            var result = await ApiClient.PutAsync(
                $"/api/v1/tutors/{_currentTutor.Id}",
                new UpdateTutorRequest
                {
                    Id = _currentTutor.Id,
                    Name = _currentTutor.Name,
                    Email = _currentTutor.Email,
                    Phone = _currentTutor.Phone
                },
                Guid.NewGuid());

            if (result.IsFailure)
            {
                Toast.Show(result.Error.Message, ToastType.Error);
                return;
            }
        }

        _showModal = false;
        Toast.Show("Tutor salvo com sucesso.", ToastType.Success);
        await LoadTutorsAsync();
    }
}
