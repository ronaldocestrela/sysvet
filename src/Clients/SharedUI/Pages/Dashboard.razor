@page "/"
@using SharedUI.Components
@using Clients.Infrastructure.Crm
@inject IVaccineStore VaccineStore

<div class="header-section">
    <h1>Dashboard</h1>
    <SyncStatusBadge />
</div>

<div class="dashboard-grid">
    <div class="card stat-card">
        <h3>Tutores</h3>
        <p class="stat-value">124</p>
    </div>
    
    <div class="card stat-card">
        <h3>Pets</h3>
        <p class="stat-value">256</p>
    </div>
    
    <div class="card stat-card">
        <h3>Consultas Hoje</h3>
        <p class="stat-value">8</p>
    </div>

    <div class="card stat-card">
        <h3>Vacinas atrasadas</h3>
        <p class="stat-value">@_overdueVaccines</p>
    </div>
</div>

@code {
    private int _overdueVaccines;

    protected override async Task OnInitializedAsync()
    {
        var result = await VaccineStore.CountOverdueAlertsAsync();
        _overdueVaccines = result.IsSuccess ? result.Value : 0;
    }
}

<style>
    .header-section {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: var(--spacing-xl);
    }
    
    .dashboard-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: var(--spacing-lg);
    }
    
    .stat-card {
        text-align: center;
    }
    
    .stat-card h3 {
        color: var(--color-text-muted);
        font-size: var(--font-size-md);
        margin-bottom: var(--spacing-sm);
    }
    
    .stat-value {
        font-size: 2.5rem;
        font-weight: var(--font-weight-bold);
        color: var(--color-primary);
        margin: 0;
    }
</style>
