name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: true
      matrix:
        dotnet: ['10.0.x']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ matrix.dotnet }}

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj', '**/dotnet-tools.json') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore
        run: dotnet restore SaaS_Veterinario.ci.slnf

      - name: Install WASM native prereqs
        run: sudo apt-get update && sudo apt-get install -y libatomic1

      - name: Install wasm-tools
        run: dotnet workload install wasm-tools

      - name: Build
        run: dotnet build SaaS_Veterinario.ci.slnf --no-restore --configuration Release

      - name: Test
        run: dotnet test SaaS_Veterinario.ci.slnf --no-build --configuration Release --settings coverage.runsettings --collect:"XPlat Code Coverage" --results-directory TestResults --logger trx -- RunConfiguration.MaxCpuCount=1

      - name: Merge coverage report
        uses: danielpalme/ReportGenerator-GitHub-Action@v5
        with:
          reports: 'TestResults/**/coverage.cobertura.xml'
          targetdir: coveragereport
          reporttypes: 'HtmlInline;Cobertura;TextSummary'
          classfilters: '+*Domain*;+*Application*;-*.Tests*'

      - name: Coverage gate (Domain + Application)
        env:
          MIN_LINE_COVERAGE: '70'
        run: bash scripts/assert-coverage.sh coveragereport/Cobertura.xml

      - name: Publish Blazor PWA
        run: |
          dotnet publish src/Clients/BlazorWeb/BlazorWeb.csproj --configuration Release --output artifacts/blazorweb
          test -f artifacts/blazorweb/wwwroot/service-worker-assets.js
          test -f artifacts/blazorweb/wwwroot/manifest.json

      - name: Publish API
        run: dotnet publish src/API/API.csproj --configuration Release --no-build --output artifacts/api

      - name: Upload API artifact
        uses: actions/upload-artifact@v4
        with:
          name: sysvet-api-release
          path: artifacts/api

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coveragereport

      - name: Validate API container build
        run: docker build -f src/API/Dockerfile .

  maui-publish:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: microsoft
          java-version: '17'

      - name: Install MAUI workload
        run: dotnet workload install maui

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-${{ runner.os }}-maui-${{ hashFiles('src/Clients/MauiApp/MauiApp.csproj') }}
          restore-keys: |
            nuget-${{ runner.os }}-maui-

      - name: Publish MAUI Windows
        run: dotnet publish src/Clients/MauiApp/MauiApp.csproj -f net10.0-windows10.0.19041.0 -c Release -o artifacts/maui-windows

      - name: Publish MAUI Android
        run: dotnet publish src/Clients/MauiApp/MauiApp.csproj -f net10.0-android -c Release -o artifacts/maui-android

      - name: Upload MAUI Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: sysvet-maui-windows
          path: artifacts/maui-windows

      - name: Upload MAUI Android artifact
        uses: actions/upload-artifact@v4
        with:
          name: sysvet-maui-android
          path: artifacts/maui-android
